/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: { TODO: enable when ready
  //   locales: ['en', 'es'],
  //   defaultLocale: 'en',
  //   localeDetection: false,
  // },
  trailingSlash: true,
}

module.exports = nextConfig
